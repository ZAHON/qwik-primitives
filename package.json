{
  "private": true,
  "name": "qwik-primitives-root",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "clean": "turbo clean && rm -rf node_modules",
    "fmt:check": "turbo fmt:check"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.0.7",
    "typescript": "^5.5.3"
  },
  "packageManager": "pnpm@9.2.0",
  "engines": {
    "node": ">=18"
  }
}
