---
import { Table } from '@/ui/astro/table';
import { parse } from './utilities';

interface Props {
  /**
   * The values of the attribute.
   */
  values: string;
}

const { values } = Astro.props;
---

<Table.Cell>
  <Fragment set:html={parse(values)} />
</Table.Cell>
